using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class StageControl : MonoBehaviour
{
    /*
    public int id;
    public GameObject[] stage;
    float x, y;
    // Start is called before the first frame update
    public void onset()
    {
        stage = new GameObject[20];
        x = 0.0f;
        y = -740.0f;
        for (int i =0; i<stage.Length; i++) 
        {
            x = Random.Range(-200.0f,200.0f);
            y +=250.0f;
            stage[i] = this.transform.GetChild(i).gameObject;

            if (i < DataManager.Instance.stage) 
            {
                stage[i].GetComponent<StageElement>().onSet(i,x,y);
            }
            else 
            {
                stage[i].GetComponent<StageElement>().offSet(i,x, y);
            }
        }
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    */
}
